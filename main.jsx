import React from "react";
import ReactDOM from "react-dom/client";

function App() {
  return <h1>chào bạn khóa 24CT2 đến với học phần CNPM-DAU</h1>;
}

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
